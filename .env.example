# ─── Session Secret (required in production) ────────────────────────────────
# Used to HMAC-sign the admin session cookie.  Without this, session cookies
# cannot be verified and all admin endpoints will reject requests.
#
# Generate a random string (32+ characters recommended):
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# In local development this can be omitted — a built-in dev-only secret is
# used automatically.  In production this MUST be set.
SESSION_SECRET=

# ─── Vercel Edge Config (private – admin credentials) ────────────────────────
# Used to store admin credentials (fast, low-latency reads via SDK).
#
# On Vercel: EDGE_CONFIG is auto-set when the store is linked to the project.
# Locally:  copy the full connection string from the Vercel dashboard into
#           EDGE_CONFIG (the ID and token are extracted automatically).
#
# Full connection string (recommended — auto-set by Vercel, or copy from dashboard):
EDGE_CONFIG=https://edge-config.vercel.com/ecfg_XXXXXXXXXX?token=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
#
# Alternative: individual pieces (backward compatible, optional):
# EDGE_CONFIG_ID=ecfg_XXXXXXXXXX
# EDGE_CONFIG_TOKEN=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
#
# Minimal setup: if you only set EDGE_CONFIG_ID + VERCEL_API_TOKEN, the app
# will auto-discover the read token from the Vercel API to enable SDK reads.

# ─── Vercel API Token (required for writing to Edge Config) ──────────────────
# The connection-string token above is READ-ONLY.  To save admin credentials
# (add admin, change password, etc.) a Vercel account-level API token is needed.
#
# Create one at: https://vercel.com/account/tokens
#   → "Create Token" → scope it to this project (or your team).
#   → Copy the token value and paste it below (or add as a Vercel env var).
#
# Without this token, admin changes are stored only in server memory and will
# be LOST when the server restarts or redeploys.
#
# NOTE: When VERCEL_API_TOKEN is set but EDGE_CONFIG / EDGE_CONFIG_ID are not,
# the app will auto-discover the Edge Config store via the Vercel REST API.
# For best results, link your Edge Config store to the project in the Vercel
# dashboard (Settings → Edge Config) so that EDGE_CONFIG is auto-set.
VERCEL_API_TOKEN=

# ─── Vercel Team ID (required for team-scoped projects) ──────────────────────
# If your Vercel project belongs to a **team** (not a personal account), the
# Vercel REST API needs a `teamId` parameter to scope requests correctly.
# Without it, Edge Config API calls may return 404.
#
# Find your Team ID in the Vercel dashboard:
#   → Team Settings → General → "Team ID" (starts with "team_")
#
# Leave unset for personal-account projects.
# VERCEL_TEAM_ID=team_XXXXXXXXXX

# ─── Vercel Blob (public – news, images, content) ───────────────────────────
# Used to store images, news content, and multilingual site-content overrides.
# NOT used for admin data — admin credentials are stored exclusively in Edge Config.
# The Blob store is public so visitors can load images/content directly.
#
# On Vercel: BLOB_READ_WRITE_TOKEN is auto-set when the store is linked.
# Locally:  copy the read-write token from the Vercel dashboard.
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_XXXXXXXXXX

# ─── Resend (password-reset emails) ──────────────────────────────────────────
# Required for the "Forgot password?" email flow on the admin login page,
# the contact form, and admin notifications.
#
# 1. Create a free account at https://resend.com
# 2. Generate an API key (API Keys → Create API Key)
# 3. Add and verify your sending domain in the Resend dashboard, then set
#    RESEND_FROM_EMAIL to an address on that domain.
#    While testing you may leave RESEND_FROM_EMAIL unset – it defaults to
#    Resend's shared sandbox address (onboarding@resend.dev).
#
# ⚠ TEST MODE (no custom domain):
#   - From address is always onboarding@resend.dev
#   - Emails can ONLY be sent to the email address registered on your Resend
#     account. Set NOTIFICATION_EMAIL to that same address.
RESEND_API_KEY=re_XXXXXXXXXX

# Optional: the "From" address used for outgoing emails.
# Must be on a domain verified in your Resend account.
# RESEND_FROM_EMAIL=noreply@yourdomain.com

# ─── Notification Email ─────────────────────────────────────────────────────
# Your personal email for receiving contact form submissions and admin
# notifications. In Resend test mode, this MUST be the same email address
# registered on your Resend account.
NOTIFICATION_EMAIL=your-email@example.com

# ─── OTP Secret (recommended in production) ──────────────────────────────────
# Used as the HMAC secret for generating/verifying one-time codes (2FA, password
# reset, recovery). If not set, falls back to RESEND_API_KEY. In production it's
# recommended to set a dedicated secret so email credential rotation doesn't
# invalidate outstanding OTP codes.
#
# Generate a random string:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# OTP_SECRET=

# ─── Recovery mode ───────────────────────────────────────────────────────────
# When set to "true", enables the recovery login flow which requires email
# verification.  Use this ONLY if you have forgotten all admin passwords.
# Steps:
#   1. Set RECOVERY_MODE=true and redeploy.
#   2. Use the recovery flow at /admin to verify your identity via email.
#   3. Create a new admin account from the admin panel.
#   4. Remove RECOVERY_MODE (or set it to "false") and redeploy again.
# RECOVERY_MODE=true
