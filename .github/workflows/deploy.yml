# This project is deployed on Vercel.
# Vercel automatically deploys on push — no GitHub Actions workflow is needed.
# See: https://vercel.com/docs/deployments/git

name: Deprecated - See Vercel

on:
  workflow_dispatch:

permissions: {}

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment notice
        run: echo "This project is deployed via Vercel. No GitHub Actions deployment is needed."
